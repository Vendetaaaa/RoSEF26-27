dataset:
  profile_path: "../artifacts/profile_dataset.json"
  leaked_bits: 12
  sequence_length: 12
  split_path: "../artifacts/dataset_split.json"

model:
  in_channels: 2
  out_channels: 8
  kernel_size: 3
  num_classes: 2

training:
  batch_size: 32
  epochs: 100
  learning_rate: 0.005
  weight_decay: 0.0001
  random_seed: 20260919
  model_save_path: "model_weights.pth"
  predictions_path: "../artifacts/cnn_predictions.json"
