Artifacts are generated by the ECDSA pipeline.

The current committed split is 96 train / 24 validation / 40 test-attack.
`cnn_predictions.json` contains only the 40 test-attack predictions.
`hnp_result.json` describes the latest CNN-driven HNP run and records the LLL backend.
`dataset_metadata.json` marks the current traces as synthetic. No physical hardware trace is claimed by these artifacts.
